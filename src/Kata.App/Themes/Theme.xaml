<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:svc="clr-namespace:Kata.App.Services"
                    xmlns:Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero2">
    <!--  Caption button (min/max/restore) — Marlett glyphs, subtle hover.
          IsSnapHover は Windows 11 のスナップレイアウトを出す都合で
          最大化ボタンだけ OS にヒットテストを譲る領域があり、そこでは
          IsMouseOver が立たない。トリガ側で代替のホバー表現を出す。  -->
    <Style x:Key="CaptionButtonStyleKey" TargetType="{x:Type Button}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Foreground" Value="{DynamicResource ControlDefaultForeground}" />
        <Setter Property="FontFamily" Value="Marlett" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Margin" Value="2" />
        <Setter Property="Padding" Value="1" />
        <Setter Property="Width" Value="{x:Static SystemParameters.WindowCaptionButtonWidth}" />
        <Setter Property="Height" Value="{x:Static SystemParameters.WindowCaptionButtonHeight}" />
        <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Border"
                            Background="Transparent"
                            BorderThickness="0"
                            CornerRadius="2"
                            TextBlock.Foreground="{TemplateBinding Foreground}">
                        <ContentPresenter Margin="2"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Focusable="False"
                                          RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background"
                                    Value="{DynamicResource ControlMouseOverBackground}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background"
                                    Value="{DynamicResource ControlBrightDefaultBackground}" />
                        </Trigger>
                        <!--  最大化ボタンの領域は OS に譲っているため IsMouseOver が立たない。
                              スナップレイアウトを出しつつホバーも見せるための別経路。  -->
                        <Trigger Property="svc:WindowChromeHelper.IsSnapHover" Value="True">
                            <Setter TargetName="Border" Property="Background"
                                    Value="{DynamicResource ControlMouseOverBackground}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Caption CLOSE button — red on hover.  -->
    <Style x:Key="CaptionCloseButtonStyleKey" TargetType="{x:Type Button}"
           BasedOn="{StaticResource CaptionButtonStyleKey}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Border"
                            Background="Transparent"
                            BorderThickness="0"
                            CornerRadius="2"
                            TextBlock.Foreground="{TemplateBinding Foreground}">
                        <ContentPresenter Margin="2"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Focusable="False"
                                          RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="Red" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="DarkRed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Chromed Window Style — MainWindow / 全ダイアログで共有。
          使い方: Style="{StaticResource ChromedWindow}" と
                svc:WindowChromeHelper.IsAttached="True" を Window に付ける。
          任意: svc:WindowChromeHelper.LogoSource / .TitleTextVisibility でロゴやタイトル表示を制御。
                Window.Tag に UIElement を置くと、タイトルバー右側のメニュー領域に載る。  -->
    <Style x:Key="ChromedWindow" TargetType="{x:Type Window}">
        <Setter Property="Background" Value="{DynamicResource BackgroundColour}" />
        <Setter Property="Foreground" Value="{DynamicResource ControlDefaultForeground}" />
        <Setter Property="TextElement.Foreground" Value="{DynamicResource ControlDefaultForeground}" />
        <Setter Property="WindowChrome.WindowChrome">
            <Setter.Value>
                <!--  UseAeroCaptionButtons は true でなければならない。
                      false だと WindowChrome が DwmDefWindowProc へ転送しなくなり、
                      WM_NCHITTEST に HTMAXBUTTON を返しても DWM がそれを知らないため、
                      Windows 11 のスナップレイアウトが出てこない。
                      キャプションボタンは自前で描いていて、システム側が重ねて
                      描くことはない (WindowChrome がキャプション領域ごと差し替えるため)。  -->
                <WindowChrome CaptionHeight="30"
                              ResizeBorderThickness="6"
                              UseAeroCaptionButtons="True"
                              CornerRadius="0"
                              GlassFrameThickness="0" />
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Window}">
                    <Border Background="{DynamicResource BackgroundColour}"
                            BorderBrush="{DynamicResource ControlDarkerBorderBrush}"
                            BorderThickness="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <DockPanel Grid.Row="0" Background="{DynamicResource WindowTitleColour}">
                                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                    <Button Command="{x:Static SystemCommands.MinimizeWindowCommand}"
                                            Content="0" Style="{StaticResource CaptionButtonStyleKey}" />
                                    <Button x:Name="MaxButton" Command="{x:Static SystemCommands.MaximizeWindowCommand}"
                                            Content="1" Style="{StaticResource CaptionButtonStyleKey}" />
                                    <Button x:Name="RestoreButton" Command="{x:Static SystemCommands.RestoreWindowCommand}"
                                            Content="2" Style="{StaticResource CaptionButtonStyleKey}"
                                            Visibility="Collapsed" />
                                    <Button Command="{x:Static SystemCommands.CloseWindowCommand}"
                                            Content="r" Style="{StaticResource CaptionCloseButtonStyleKey}" />
                                </StackPanel>
                                <!--  キャプションボタンの左隣に載せる領域。開いているソリューション名など、
                                      右端に寄せて出したい情報の置き場。中身が null なら幅ゼロ。  -->
                                <ContentControl DockPanel.Dock="Right"
                                                VerticalAlignment="Center"
                                                Margin="0,0,8,0"
                                                Content="{Binding Path=(svc:WindowChromeHelper.RightHeaderContent), RelativeSource={RelativeSource TemplatedParent}}" />
                                <!--  ロゴ画像は Window の svc:WindowChromeHelper.LogoSource から拾う。
                                      未指定なら幅ゼロなので何も出ない。IsHitTestVisible=False で
                                      DockPanel 背景 (WindowChrome のドラッグ領域) に落とす。  -->
                                <Image x:Name="AppLogoImage" DockPanel.Dock="Left"
                                       Height="22" MaxWidth="140" Margin="10,0,0,0"
                                       VerticalAlignment="Center"
                                       Stretch="Uniform"
                                       IsHitTestVisible="False"
                                       Source="{Binding Path=(svc:WindowChromeHelper.LogoSource), RelativeSource={RelativeSource TemplatedParent}}" />
                                <TextBlock x:Name="AppTitleText" DockPanel.Dock="Left"
                                           Text="{TemplateBinding Title}" FontWeight="SemiBold"
                                           Foreground="{DynamicResource ControlDefaultForeground}"
                                           VerticalAlignment="Center"
                                           Margin="8,0,8,0"
                                           IsHitTestVisible="False"
                                           Visibility="{Binding Path=(svc:WindowChromeHelper.TitleTextVisibility), RelativeSource={RelativeSource TemplatedParent}}" />
                                <!--  Window.Tag の内容 (メニュー等) を左詰めで載せる。中身自身が
                                      WindowChrome.IsHitTestVisibleInChrome=True を立てる。  -->
                                <ContentControl Content="{TemplateBinding Tag}"
                                                HorizontalContentAlignment="Left"
                                                VerticalAlignment="Center" />
                            </DockPanel>
                            <AdornerDecorator Grid.Row="1">
                                <ContentPresenter Content="{TemplateBinding Content}" />
                            </AdornerDecorator>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Color x:Key="GlowBlue">#FF2084E8</Color>
    <Color x:Key="GlowBlueBright">#FF50A4FA</Color>

    <Style x:Key="DarkMessageBoxButton" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="{DynamicResource ControlDefaultBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ControlDefaultBorderBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource ControlDefaultForeground}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="BorderThickness" Value="1.5" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Padding" Value="1" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border
                        x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="2"
                        SnapsToDevicePixels="True">
                        <Border.Effect>
                            <DropShadowEffect
                                x:Name="glowEffect"
                                BlurRadius="0"
                                Opacity="0"
                                ShadowDepth="0"
                                Color="{StaticResource GlowBlue}" />
                        </Border.Effect>
                        <ContentPresenter
                            x:Name="contentPresenter"
                            Margin="{TemplateBinding Padding}"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Focusable="False"
                            RecognizesAccessKey="True"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation
                                            Storyboard.TargetName="border"
                                            Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                            To="{StaticResource GlowBlueBright}"
                                            Duration="0:0:0.15" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="glowEffect"
                                            Storyboard.TargetProperty="BlurRadius"
                                            To="12"
                                            Duration="0:0:0.15" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="glowEffect"
                                            Storyboard.TargetProperty="Opacity"
                                            To="0.6"
                                            Duration="0:0:0.15" />
                                        <ColorAnimation
                                            Storyboard.TargetName="border"
                                            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                            To="#FF383838"
                                            Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation
                                            Storyboard.TargetName="border"
                                            Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                            Duration="0:0:0.25" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="glowEffect"
                                            Storyboard.TargetProperty="BlurRadius"
                                            To="0"
                                            Duration="0:0:0.25" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="glowEffect"
                                            Storyboard.TargetProperty="Opacity"
                                            To="0"
                                            Duration="0:0:0.25" />
                                        <ColorAnimation
                                            Storyboard.TargetName="border"
                                            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                            Duration="0:0:0.25" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation
                                            Storyboard.TargetName="border"
                                            Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                            To="{StaticResource GlowBlue}"
                                            Duration="0:0:0.15" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="glowEffect"
                                            Storyboard.TargetProperty="BlurRadius"
                                            To="8"
                                            Duration="0:0:0.15" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="glowEffect"
                                            Storyboard.TargetProperty="Opacity"
                                            To="0.45"
                                            Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation
                                            Storyboard.TargetName="border"
                                            Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                            Duration="0:0:0.25" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="glowEffect"
                                            Storyboard.TargetProperty="BlurRadius"
                                            To="0"
                                            Duration="0:0:0.25" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="glowEffect"
                                            Storyboard.TargetProperty="Opacity"
                                            To="0"
                                            Duration="0:0:0.25" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Color="#FF1A6CC0" />
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="border" Property="BorderBrush">
                                <Setter.Value>
                                    <SolidColorBrush Color="{StaticResource GlowBlueBright}" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background"
                                    Value="{DynamicResource ControlBrightDefaultBackground}" />
                            <Setter TargetName="border" Property="BorderBrush"
                                    Value="{DynamicResource ControlBrightDefaultBorderBrush}" />
                            <Setter TargetName="contentPresenter" Property="TextElement.Foreground"
                                    Value="{DynamicResource ControlDisabledGlythColour}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Fader: transparent repeat button  -->
    <Style x:Key="FaderRepeatButton" TargetType="{x:Type RepeatButton}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Rectangle Fill="Transparent" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Fader: horizontal thumb (fader cap) with value popup  -->
    <ControlTemplate x:Key="FaderThumbHorizontal" TargetType="{x:Type Thumb}">
        <Grid SnapsToDevicePixels="True" UseLayoutRounding="True">
            <!--  Value popup: shown while dragging  -->
            <Popup
                x:Name="valuePopup"
                AllowsTransparency="True"
                DataContext="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                IsHitTestVisible="False"
                IsOpen="False"
                Placement="Top"
                PlacementTarget="{Binding ElementName=thumbBorder}"
                PopupAnimation="Fade"
                VerticalOffset="-4">
                <Border
                    Margin="0,0,0,2"
                    Padding="8,4"
                    Background="#F0181818"
                    BorderBrush="{StaticResource ControlPrimaryColourBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="4">
                    <Border.Effect>
                        <DropShadowEffect
                            BlurRadius="6"
                            Direction="270"
                            Opacity="0.5"
                            ShadowDepth="2"
                            Color="Black" />
                    </Border.Effect>
                    <TextBlock
                        FontSize="11"
                        FontWeight="SemiBold"
                        Foreground="{StaticResource ControlPrimaryColourBorderBrush}"
                        Text="{Binding Tag, StringFormat='{}{0:P0}'}"
                        TextAlignment="Center" />
                </Border>
            </Popup>
            <!--  Fader cap  -->
            <Border
                x:Name="thumbBorder"
                Background="#FF4A4A4A"
                BorderBrush="#FF606060"
                BorderThickness="1"
                CornerRadius="2">
                <Grid>
                    <!--  Center groove line  -->
                    <Rectangle
                        Width="1"
                        Height="8"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Fill="#FF808080" />
                </Grid>
            </Border>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="thumbBorder" Property="Background" Value="#FF585858" />
                <Setter TargetName="thumbBorder" Property="BorderBrush" Value="#FF707070" />
            </Trigger>
            <Trigger Property="IsDragging" Value="True">
                <Setter TargetName="thumbBorder" Property="Background" Value="#FF656565" />
                <Setter TargetName="thumbBorder" Property="BorderBrush"
                        Value="{StaticResource ControlPrimaryColourBorderBrush}" />
                <Setter TargetName="valuePopup" Property="IsOpen" Value="True" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="thumbBorder" Property="Background" Value="#FF353535" />
                <Setter TargetName="thumbBorder" Property="BorderBrush" Value="#FF3A3A3A" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!--  Fader: vertical thumb (fader cap) with value popup  -->
    <ControlTemplate x:Key="FaderThumbVertical" TargetType="{x:Type Thumb}">
        <Grid SnapsToDevicePixels="True" UseLayoutRounding="True">
            <!--  Value popup: shown while dragging  -->
            <Popup
                x:Name="valuePopup"
                AllowsTransparency="True"
                DataContext="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                HorizontalOffset="-4"
                IsHitTestVisible="False"
                IsOpen="False"
                Placement="Left"
                PlacementTarget="{Binding ElementName=thumbBorder}"
                PopupAnimation="Fade">
                <Border
                    Margin="0,0,2,0"
                    Padding="8,4"
                    Background="#F0181818"
                    BorderBrush="{StaticResource ControlPrimaryColourBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="4">
                    <Border.Effect>
                        <DropShadowEffect
                            BlurRadius="6"
                            Direction="270"
                            Opacity="0.5"
                            ShadowDepth="2"
                            Color="Black" />
                    </Border.Effect>
                    <TextBlock
                        FontSize="11"
                        FontWeight="SemiBold"
                        Foreground="{StaticResource ControlPrimaryColourBorderBrush}"
                        Text="{Binding Tag, StringFormat='{}{0:P0}'}"
                        TextAlignment="Center" />
                </Border>
            </Popup>
            <!--  Fader cap  -->
            <Border
                x:Name="thumbBorder"
                Background="#FF4A4A4A"
                BorderBrush="#FF606060"
                BorderThickness="1"
                CornerRadius="2">
                <Grid>
                    <!--  Center groove line  -->
                    <Rectangle
                        Width="8"
                        Height="1"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Fill="#FF808080" />
                </Grid>
            </Border>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="thumbBorder" Property="Background" Value="#FF585858" />
                <Setter TargetName="thumbBorder" Property="BorderBrush" Value="#FF707070" />
            </Trigger>
            <Trigger Property="IsDragging" Value="True">
                <Setter TargetName="thumbBorder" Property="Background" Value="#FF656565" />
                <Setter TargetName="thumbBorder" Property="BorderBrush"
                        Value="{StaticResource ControlPrimaryColourBorderBrush}" />
                <Setter TargetName="valuePopup" Property="IsOpen" Value="True" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="thumbBorder" Property="Background" Value="#FF353535" />
                <Setter TargetName="thumbBorder" Property="BorderBrush" Value="#FF3A3A3A" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!--  Fader: horizontal slider template  -->
    <ControlTemplate x:Key="FaderHorizontal" TargetType="{x:Type Slider}">
        <Border
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            SnapsToDevicePixels="True">
            <Grid>
                <!--  Track groove  -->
                <Border
                    Height="2"
                    Margin="4,0"
                    VerticalAlignment="Center"
                    Background="#FF1A1A1A"
                    BorderBrush="#FF2A2A2A"
                    BorderThickness="1"
                    CornerRadius="1" />
                <Track x:Name="PART_Track">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Command="{x:Static Slider.DecreaseLarge}">
                            <RepeatButton.Template>
                                <ControlTemplate TargetType="RepeatButton">
                                    <Border
                                        Height="2"
                                        VerticalAlignment="Center"
                                        Background="{StaticResource ControlPrimaryColourBackground}"
                                        CornerRadius="1" />
                                </ControlTemplate>
                            </RepeatButton.Template>
                        </RepeatButton>
                    </Track.DecreaseRepeatButton>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Command="{x:Static Slider.IncreaseLarge}"
                                      Style="{StaticResource FaderRepeatButton}" />
                    </Track.IncreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb
                            x:Name="Thumb"
                            Width="8"
                            Height="16"
                            Focusable="False"
                            OverridesDefaultStyle="True"
                            Tag="{Binding Value, RelativeSource={RelativeSource AncestorType=Slider}}"
                            Template="{StaticResource FaderThumbHorizontal}" />
                    </Track.Thumb>
                </Track>
            </Grid>
        </Border>
    </ControlTemplate>

    <!--  Fader: vertical slider template  -->
    <ControlTemplate x:Key="FaderVertical" TargetType="{x:Type Slider}">
        <Border
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            SnapsToDevicePixels="True">
            <Grid>
                <!--  Track groove  -->
                <Border
                    Width="2"
                    Margin="0,4"
                    HorizontalAlignment="Center"
                    Background="#FF1A1A1A"
                    BorderBrush="#FF2A2A2A"
                    BorderThickness="1"
                    CornerRadius="1" />
                <Track x:Name="PART_Track">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Command="{x:Static Slider.DecreaseLarge}">
                            <RepeatButton.Template>
                                <ControlTemplate TargetType="RepeatButton">
                                    <Border
                                        Width="2"
                                        HorizontalAlignment="Center"
                                        Background="{StaticResource ControlPrimaryColourBackground}"
                                        CornerRadius="1" />
                                </ControlTemplate>
                            </RepeatButton.Template>
                        </RepeatButton>
                    </Track.DecreaseRepeatButton>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Command="{x:Static Slider.IncreaseLarge}"
                                      Style="{StaticResource FaderRepeatButton}" />
                    </Track.IncreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb
                            x:Name="Thumb"
                            Width="16"
                            Height="8"
                            Focusable="False"
                            OverridesDefaultStyle="True"
                            Tag="{Binding Value, RelativeSource={RelativeSource AncestorType=Slider}}"
                            Template="{StaticResource FaderThumbVertical}" />
                    </Track.Thumb>
                </Track>
            </Grid>
        </Border>
    </ControlTemplate>

    <!--  Fader style for volume sliders  -->
    <Style x:Key="FaderSliderStyle" TargetType="{x:Type Slider}">
        <Setter Property="Stylus.IsPressAndHoldEnabled" Value="True" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Template" Value="{StaticResource FaderHorizontal}" />
        <Setter Property="IsMoveToPointEnabled" Value="True" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Template" Value="{StaticResource FaderVertical}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--  Menu (他プロジェクト由来)。トップレベルの Menu 自体は WindowTitleColour
          で塗って、タイトルバーの並びと自然につなげる。  -->
    <Style TargetType="{x:Type Menu}">
        <Setter Property="Background" Value="{DynamicResource WindowTitleColour}" />
        <Setter Property="Foreground" Value="{DynamicResource ControlDefaultForeground}" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Menu}">
                    <Border Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  メニュー内の区切り。既定の Separator (2px の凸凹) だと暗色テーマで
          浮いて見えるので、1px の平坦な線に差し替える。  -->
    <Style TargetType="{x:Type Separator}">
        <Setter Property="Height" Value="1" />
        <Setter Property="Margin" Value="4,4" />
        <Setter Property="Background" Value="{DynamicResource ControlDefaultBorderBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Separator}">
                    <Border Height="1" Background="{TemplateBinding Background}" SnapsToDevicePixels="True" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  サブメニュー内の葉ノード (SubmenuItem role) 用テンプレート。  -->
    <ControlTemplate x:Key="SingleDropDownMenuItem" TargetType="{x:Type MenuItem}">
        <Border x:Name="templateRoot"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                SnapsToDevicePixels="True">
            <Grid Margin="-1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="22" SharedSizeGroup="MenuItemIconColumnGroup" />
                    <ColumnDefinition Width="13" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="30" />
                    <ColumnDefinition Width="Auto" SharedSizeGroup="MenuItemIGTColumnGroup" />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>
                <ContentPresenter x:Name="Icon" Width="16" Height="16" Margin="3"
                                  HorizontalAlignment="Center" VerticalAlignment="Center"
                                  Content="{TemplateBinding Icon}" ContentSource="Icon"
                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                <Border x:Name="GlyphPanel" Width="22" Height="22" Margin="-1,0,0,0"
                        VerticalAlignment="Center"
                        Background="{DynamicResource MenuItem.Highlight.Background}"
                        BorderBrush="{DynamicResource MenuItem.Highlight.Border}"
                        BorderThickness="1"
                        Visibility="Hidden">
                    <Path x:Name="Glyph" Width="9" Height="11"
                          Data="F1M10,1.2L4.7,9.1 4.5,9.1 0,5.2 1.3,3.5 4.3,6.1 8.3,0 10,1.2z"
                          Fill="{DynamicResource MenuItem.Glyph.Foreground}"
                          FlowDirection="LeftToRight" />
                </Border>
                <ContentPresenter Grid.Column="2"
                                  Margin="{TemplateBinding Padding}"
                                  HorizontalAlignment="Left" VerticalAlignment="Center"
                                  Content="{TemplateBinding Header}" ContentSource="Header"
                                  ContentStringFormat="{TemplateBinding HeaderStringFormat}"
                                  ContentTemplate="{TemplateBinding HeaderTemplate}"
                                  RecognizesAccessKey="True"
                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                <TextBlock Grid.Column="4" Margin="{TemplateBinding Padding}"
                           VerticalAlignment="Center" Opacity="0.7"
                           Text="{TemplateBinding InputGestureText}" />
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="Icon" Value="{x:Null}">
                <Setter TargetName="Icon" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="GlyphPanel" Property="Visibility" Value="Visible" />
                <Setter TargetName="Icon" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="templateRoot" Property="Background" Value="{DynamicResource MenuItem.Highlight.Background}" />
                <Setter TargetName="templateRoot" Property="BorderBrush" Value="{DynamicResource MenuItem.Highlight.Border}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="templateRoot" Property="TextElement.Foreground" Value="{DynamicResource ControlDisabledGlythColour}" />
                <Setter TargetName="Glyph" Property="Fill" Value="{DynamicResource ControlDisabledGlythColour}" />
                <Setter Property="Background" Value="{DynamicResource ControlDisabledBackground}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!--  サブメニューを持つ MenuItem (SubmenuHeader role) 用テンプレート。
          右向き三角形 + Popup 付き。  -->
    <ControlTemplate x:Key="DropDownMenuItemStyle" TargetType="{x:Type MenuItem}">
        <Border x:Name="templateRoot"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                SnapsToDevicePixels="True">
            <Grid Margin="-1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="22" SharedSizeGroup="MenuItemIconColumnGroup" />
                    <ColumnDefinition Width="13" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="30" />
                    <ColumnDefinition Width="Auto" SharedSizeGroup="MenuItemIGTColumnGroup" />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>
                <ContentPresenter x:Name="Icon" Width="16" Height="16" Margin="3"
                                  HorizontalAlignment="Center" VerticalAlignment="Center"
                                  Content="{TemplateBinding Icon}" ContentSource="Icon"
                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                <Border x:Name="GlyphPanel" Width="22" Height="22" Margin="-1,0,0,0"
                        VerticalAlignment="Center"
                        Background="{DynamicResource MenuItem.Highlight.Background}"
                        BorderBrush="{DynamicResource MenuItem.Highlight.Border}"
                        BorderThickness="1"
                        Visibility="Hidden">
                    <Path x:Name="Glyph" Width="9" Height="11"
                          Data="F1M10,1.2L4.7,9.1 4.5,9.1 0,5.2 1.3,3.5 4.3,6.1 8.3,0 10,1.2z"
                          Fill="{DynamicResource MenuItem.Glyph.Foreground}"
                          FlowDirection="LeftToRight" />
                </Border>
                <ContentPresenter Grid.Column="2"
                                  Margin="{TemplateBinding Padding}"
                                  HorizontalAlignment="Left" VerticalAlignment="Center"
                                  Content="{TemplateBinding Header}" ContentSource="Header"
                                  ContentStringFormat="{TemplateBinding HeaderStringFormat}"
                                  ContentTemplate="{TemplateBinding HeaderTemplate}"
                                  RecognizesAccessKey="True"
                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                <TextBlock Grid.Column="4" Margin="{TemplateBinding Padding}"
                           VerticalAlignment="Center" Opacity="0.7"
                           Text="{TemplateBinding InputGestureText}" />
                <Path x:Name="RightArrow" Grid.Column="5" Margin="10,0,0,0"
                      HorizontalAlignment="Left" VerticalAlignment="Center"
                      Data="M0,0L4,3.5 0,7z"
                      Fill="{DynamicResource ControlGlythColour}" />
                <Popup x:Name="PART_Popup" AllowsTransparency="True" Focusable="False"
                       HorizontalOffset="-2"
                       IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                       Placement="Right" PopupAnimation="Fade" VerticalOffset="-3">
                    <Themes:SystemDropShadowChrome Name="Shdw" Margin="0,0,5,5"
                                                   Themes:SystemDropShadowChrome.Color="{DynamicResource PopupShadowColor}"
                                                   Themes:SystemDropShadowChrome.CornerRadius="0"
                                                   SnapsToDevicePixels="True">
                        <Border x:Name="SubMenuBorder" Padding="0"
                                Background="{DynamicResource ControlDefaultBackground}"
                                BorderBrush="{DynamicResource ControlDefaultBorderBrush}"
                                BorderThickness="1">
                            <Grid RenderOptions.ClearTypeHint="Enabled">
                                <Canvas Width="22" Height="Auto"
                                        HorizontalAlignment="Left" VerticalAlignment="Stretch">
                                    <Rectangle Width="{Binding ActualWidth, ElementName=SubMenuBorder}"
                                               Height="{Binding ActualHeight, ElementName=SubMenuBorder}"
                                               Fill="{Binding Background, ElementName=SubMenuBorder}" />
                                </Canvas>
                                <Rectangle Width="1" Margin="22,0,0,0"
                                           HorizontalAlignment="Left"
                                           Fill="{DynamicResource ControlDarkerBorderBrush}" />
                                <Rectangle Width="22" Margin="0" HorizontalAlignment="Left"
                                           Fill="{Binding Background, ElementName=SubMenuBorder}" />
                                <ItemsPresenter Grid.IsSharedSizeScope="True"
                                                KeyboardNavigation.DirectionalNavigation="Cycle"
                                                KeyboardNavigation.TabNavigation="Cycle"
                                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Grid>
                        </Border>
                    </Themes:SystemDropShadowChrome>
                </Popup>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsSuspendingPopupAnimation" Value="True">
                <Setter TargetName="PART_Popup" Property="PopupAnimation" Value="None" />
            </Trigger>
            <Trigger Property="Icon" Value="{x:Null}">
                <Setter TargetName="Icon" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="GlyphPanel" Property="Visibility" Value="Visible" />
                <Setter TargetName="Icon" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="templateRoot" Property="Background" Value="{DynamicResource MenuItem.Highlight.Background}" />
                <Setter TargetName="templateRoot" Property="BorderBrush" Value="{DynamicResource MenuItem.Highlight.Border}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="templateRoot" Property="TextElement.Foreground" Value="{DynamicResource ControlDisabledGlythColour}" />
                <Setter TargetName="Glyph" Property="Fill" Value="{DynamicResource ControlDisabledGlythColour}" />
                <Setter TargetName="RightArrow" Property="Fill" Value="{DynamicResource ControlDisabledGlythColour}" />
                <Setter Property="Background" Value="{DynamicResource ControlDisabledBackground}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!--  MenuItem 基本 (TopLevelHeader / TopLevelItem)。Role によって
          テンプレートを DropDownMenuItemStyle / SingleDropDownMenuItem に切り替える。  -->
    <Style TargetType="{x:Type MenuItem}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource ControlDefaultForeground}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="6,3" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type MenuItem}">
                    <Border x:Name="templateRoot"
                            Background="{TemplateBinding Background}"
                            BorderBrush="Transparent"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2"
                            SnapsToDevicePixels="True">
                        <Grid VerticalAlignment="Center">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ContentPresenter x:Name="Icon" Width="16" Height="16" Margin="3"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Content="{TemplateBinding Icon}" ContentSource="Icon"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            <Path x:Name="GlyphPanel" Margin="3" VerticalAlignment="Center"
                                  Data="F1M10,1.2L4.7,9.1 4.5,9.1 0,5.2 1.3,3.5 4.3,6.1 8.3,0 10,1.2z"
                                  Fill="{TemplateBinding Foreground}"
                                  FlowDirection="LeftToRight" Visibility="Collapsed" />
                            <ContentPresenter Grid.Column="1" Margin="{TemplateBinding Padding}"
                                              Content="{TemplateBinding Header}" ContentSource="Header"
                                              ContentStringFormat="{TemplateBinding HeaderStringFormat}"
                                              ContentTemplate="{TemplateBinding HeaderTemplate}"
                                              RecognizesAccessKey="True"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            <Popup x:Name="PART_Popup" AllowsTransparency="True" Focusable="False"
                                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   Placement="Bottom" PopupAnimation="Fade">
                                <Themes:SystemDropShadowChrome Name="Shdw" Margin="0,0,5,5"
                                                               Themes:SystemDropShadowChrome.Color="{DynamicResource PopupShadowColor}"
                                                               Themes:SystemDropShadowChrome.CornerRadius="0"
                                                               SnapsToDevicePixels="True">
                                    <Border x:Name="SubMenuBorder" Padding="0"
                                            Background="{DynamicResource ControlDefaultBackground}"
                                            BorderBrush="{DynamicResource ControlDefaultBorderBrush}"
                                            BorderThickness="1">
                                        <Grid RenderOptions.ClearTypeHint="Enabled">
                                            <Canvas Width="22" Height="Auto"
                                                    HorizontalAlignment="Left" VerticalAlignment="Stretch">
                                                <Rectangle Width="{Binding ActualWidth, ElementName=SubMenuBorder}"
                                                           Height="{Binding ActualHeight, ElementName=SubMenuBorder}"
                                                           Fill="{Binding Background, ElementName=SubMenuBorder}" />
                                            </Canvas>
                                            <Rectangle Width="1" Margin="22,0,0,0"
                                                       HorizontalAlignment="Left"
                                                       Fill="{DynamicResource ControlDarkerBorderBrush}" />
                                            <Rectangle Width="22" Margin="0" HorizontalAlignment="Left"
                                                       Fill="{Binding Background, ElementName=SubMenuBorder}" />
                                            <ItemsPresenter Grid.IsSharedSizeScope="True"
                                                            KeyboardNavigation.DirectionalNavigation="Cycle"
                                                            KeyboardNavigation.TabNavigation="Cycle"
                                                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                        </Grid>
                                    </Border>
                                </Themes:SystemDropShadowChrome>
                            </Popup>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSuspendingPopupAnimation" Value="True">
                            <Setter TargetName="PART_Popup" Property="PopupAnimation" Value="None" />
                        </Trigger>
                        <Trigger Property="Icon" Value="{x:Null}">
                            <Setter TargetName="Icon" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="GlyphPanel" Property="Visibility" Value="Visible" />
                            <Setter TargetName="Icon" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="templateRoot" Property="Background" Value="{DynamicResource MenuItem.Highlight.Background}" />
                            <Setter TargetName="templateRoot" Property="BorderBrush" Value="{DynamicResource MenuItem.Highlight.Border}" />
                        </Trigger>
                        <Trigger Property="IsSubmenuOpen" Value="True">
                            <Setter TargetName="templateRoot" Property="Background" Value="{DynamicResource MenuItem.Highlight.Background}" />
                            <Setter TargetName="templateRoot" Property="BorderBrush" Value="{DynamicResource MenuItem.Highlight.Border}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="templateRoot" Property="TextElement.Foreground" Value="{DynamicResource ControlDisabledGlythColour}" />
                            <Setter Property="Background" Value="{DynamicResource ControlDisabledBackground}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Role" Value="SubmenuHeader">
                <Setter Property="Template" Value="{StaticResource DropDownMenuItemStyle}" />
            </Trigger>
            <Trigger Property="Role" Value="SubmenuItem">
                <Setter Property="Template" Value="{StaticResource SingleDropDownMenuItem}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--  ContextMenu — WPF 既定テンプレートには左端に白 gutter (Canvas 内の OpaqueRect)
          が入ってしまうため、テンプレートを差し替えて除去する。  -->
    <Style TargetType="{x:Type ContextMenu}">
        <Setter Property="Background" Value="{DynamicResource ContainerBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ContainerBorder}" />
        <Setter Property="Foreground" Value="{DynamicResource ControlDefaultForeground}" />
        <Setter Property="Padding" Value="2" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ContextMenu}">
                    <Themes:SystemDropShadowChrome Name="Shdw"
                                                   Margin="0,0,5,5"
                                                   Themes:SystemDropShadowChrome.Color="{DynamicResource PopupShadowColor}"
                                                   Themes:SystemDropShadowChrome.CornerRadius="0"
                                                   SnapsToDevicePixels="True">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle"
                                            KeyboardNavigation.TabNavigation="Cycle"
                                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Border>
                    </Themes:SystemDropShadowChrome>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>